$ cargo fmt --all -- --check
Diff in /Users/shinyoohag/Desktop/frametrace/src/qa_tests/accuracy.rs:150:
         "ffprobe-video-stream-confirmed"
     );
     assert_eq!(
-        json["domains"][1]["status"],
-        "unsupported",
+        json["domains"][1]["status"], "unsupported",
         "unsupported domains must be recorded as unsupported, not pass"
     );
     assert_eq!(json["release_keys"]["mixed_real_world_like"], "unsupported");
Diff in /Users/shinyoohag/Desktop/frametrace/src/qa_tests/reproducibility_performance.rs:69:
     let json = read_json(&report.report_path);
 
     assert!(report.passed);
-    assert_eq!(json["allowed_diff_thresholds"]["normalized_core_differences"], 0);
+    assert_eq!(
+        json["allowed_diff_thresholds"]["normalized_core_differences"],
+        0
+    );
     assert_eq!(json["diff_metrics"]["normalized_core_differences"], 0);
-    assert_eq!(json["diff_metrics"]["normalized_left_bytes"], json["normalized_left_bytes"]);
-    assert_eq!(json["diff_metrics"]["normalized_right_bytes"], json["normalized_right_bytes"]);
+    assert_eq!(
+        json["diff_metrics"]["normalized_left_bytes"],
+        json["normalized_left_bytes"]
+    );
+    assert_eq!(
+        json["diff_metrics"]["normalized_right_bytes"],
+        json["normalized_right_bytes"]
+    );
 
     let _ = fs::remove_dir_all(root);
 }
EXIT_CODE=1
