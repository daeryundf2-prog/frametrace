# FrameTrace 로드맵 (2026-09-08 v2 — 3차 하드닝 이후 재작성)

> v1 로드맵(M1~M4, 2026-08-30)은 달성·초과 완료되어 아카이브 성격으로 유지.
> 이 문서는 3차례 심층 리뷰(serde 계약 / 보안·강건성 / 미커버 모듈+회귀)와
> 전 항목 수정 이후의 현재 상태에서 다시 작성했다.
> 상용 비교 기준점 유지: Magnet DVR Examiner, Amped FIVE, Oxygen, Autopsy.

## 0. 포지셔닝 (불변)

**"확보된 영상 증거(표준 파일·E01·복구물)를 판독 → 검증 → 방어 가능한
보고서로 마무리하는 로컬 워크스테이션."** 경쟁하지 않는 축(독포맷 100종,
위변조 탐지)은 연동으로 대응. 이기는 축(체인 해시 감사, 무료·로컬·한글)은
그대로.

## 1. 현재 상태 (2026-09-08, v0.5.0+3커밋 하드닝)

**종합 8.2/10** (v1 로드맵 M3 목표 8.0+ 초과 달성)

- 완료: M1 신뢰성, M2 복원력, M3 제품화, M4 1차(0.5.0), serde 2단계,
  DAV 프레임 간격 anomaly, 3차례 심층 리뷰 하드닝(CRITICAL 4건·HIGH 8건·
  MEDIUM 10건·LOW 다수 전부 수정 또는 문서화).
- 품질 지표: 테스트 151(단위 143 + 스모크 3 + 실도구 IT 4 + 워크스테이션
  E2E 1) + perf harness 3(opt-in) · release
  readiness 6게이트(감사체인·교차일관성 포함) 전 PASS · 성능 예산 전
  통과(1천 건 썸네일 13s/180s, 검증 32s/240s, DB 10k 591ms).
- 핵심 보증: 감사로그가 빈/거짓 산출물을 보증하지 않음(0바이트 차단),
  다중 소스 워크스테이션에서 증거 상태 무오류(디스크 검사 기반 stale).

## 2. 다음 마일스톤

### M5 "실무 투입" (외부 전제 해소 시 착수) — 종합 8.2 → 8.6

| 항목 | 전제 | 완료 기준 |
|---|---|---|
| 실장비 DAV/Hik 코퍼스 검증 | DVR 3종 이상 녹화 샘플 | 인테이크 하니스 exit 0, 코퍼스 리포트 작성 |
| DAV walk_frames resync 정책 | 동일 (손상 프레임 샘플) | FFmpeg resync와 동등한 부분 복구 + 스킵 로그 |
| 감사 체인 HMAC 옵션 | 설계 결정 (무키 한계 법정 공개) | opt-in 키 체인, 문서에 한계 명시 |
| Hik HDD FS (`HIKVISION@HANGZHOU`) | HDD 이미지 | 코퍼스 없으면 착수 금지 원칙 유지 |

### M6 "운영 안정화" (무전제, 즉시 착수 가능) — 종합 8.2 → 8.4

> **상태: M6-2/3/4 완료 (2026-09-08).** M6-1은 Windows 머신 전제로 이월.

1. **WinUI 풀 GUI** — 얇은 셸(`gui/winui`)이 이미 동작하므로 뷰어 워크플로
   (INPUT→스캔→마크→보고서)를 네이티브로. 5~8일.
   > **상태: Windows 머신 전제 이월.** 단, 세션 중 워크스테이션 파이프라인이
   > `frametrace-app`을 CLI로 spawn할 때 app이 인자를 무시하고 또다른
   > 워크스테이션을 띄우던 **치명적 버그를 발견·수정**했다 (app.rs가 CLI
   > 서브커맨드/포트 인자 위임). 이 버그가 WinUI 셸 위임 경로의 실질
   > 장애물이었다. 나머지 풀 GUI 스캐폴드는 Windows SDK 필요.
2. **워크스테이션 E2E IT** — 브라우저 없이 HTTP 클라이언트로 5단계
   파이프라인을 실도구로 통과시키는 통합 테스트(현재는 단위+수동 스모크).
   > **상태: 완료.** `tests/workstation_e2e.rs` — 실 `frametrace-app` 기동
   > (FRAMETRACE_NO_BROWSER 지원 추가), HTTP로 api/start → 5단계
   > running → review-ready → finalize → done 완주 (0.8s), 뷰어·리포트·
   > 감사로그 artifact 단정. IT 5번째 테스트로 편입.
3. **CI에 macOS 레인 추가** — `which` 수정으로 IT가 유닉스에서 돌지만
   CI는 Windows뿐. dual-OS 게이트.
   > **상태: 완료.** `.github/workflows/macos-ci.yml` (arm64): fmt/clippy/
   > node 게이트 + brew ffmpeg·libewf·sleuthkit + IT + **워크스테이션
   > E2E** + 성능 스모크 + release 빌드. Windows CI에도 E2E 스텝 추가.
4. **carve 중복 후보 스트리밍 해시** — 전체 기록 후 sha256 비교 대신
   스트리밍으로 조기 스킵(대용량 디스크 볼륨 절감).
   > **상태: 판단 변경 — 구현하지 않음.** 카빙 산출물은 포렌식 증거로
   > **디스크에 기록 후 해시**하는 순서 자체가 보존 계약이다. 중복 후보를
   > 디스크에 남기는 것도 의도된 동작(중복 역시 증거). 대신 죽은 분기
   > (next_offset <= hit.offset, hits 정렬·dedup으로 도달 불가) 제거,
   > candidate-limit 경고가 비절단에도 발생하던 `>=` → `>` 수정.

### 백로그 (우선순위 낮음)

- 리포트 숫자 필드 이스케이프 균일화(L2), 썸네일 캐시 콘텐츠 키(L4),
  `file_url` scheme 방어(L5), 스키마 재적용 스킵(L6) — 전부 LOW.
- `docs/MANUFACTURER_PARSER_RESEARCH.md`의 다음 레인(코퍼스 확보 가능할 때만).

## 3. 추적 지표

- 스코어: 현재 8.2 · M6-1(Windows 머신) 후 8.4 · M5 후 8.6 (9.0은 실무 필드 투입 실적 필요)
- 성능 예산: 1만 건 뷰어 로드 ≤1.5s · 1천 썸네일 ≤180s · 1천 검증 ≤240s ·
  inspect-image 1만 엔트리 ≤120s (타임아웃 상한 신설)
- 품질: 테스트 ≥130(현재 151: 단위 143 + 스모크 3 + 실도구 IT 4 +
  워크스테이션 E2E 1) · release 6게이트 적색 0 · CI dual-OS(macOS 레인
  신설) 적색 0
- 무결성: 모든 감사로그 `verify-audit` 100% · `qa consistency` 100% ·
  0바이트 산출물 기록 0건(가드 상시)

## 4. 열린 결정사항

| # | 결정 | 상태 |
|---|---|---|
| 1 | 감사 체인 HMAC | M5 후보 — 도입 여부는 법실무 자문과 함께 |
| 2 | DAV resync 정책 | 실샘플 확보 시 FFmpeg 동등성 기준으로 결정 |
| 3 | WinUI 풀 GUI 시점 | M6-1 — 브라우저 런처 실무 사용 피드백 우선 |

## 5. 아카이브

- v1 로드맵(M1~M4 상세): `docs/ROADMAP.md` §3~§6 (달성 기록으로 유지)
- 리뷰·수정 상세: `docs/REVIEW_FIXES_2026-09-08.md` (1~3차 전원)
- macOS 풀레인 영수증: `docs/MACOS_FULL_RUN_RECEIPT.md`
